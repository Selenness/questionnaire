h2 Your email:
=form_for(User.new, url: confirm_email_path, html: { method: :post }) do |f|
  =f.text_area :email
  =f.submit 'Confirm email'
  =hidden_field_tag :provider, @provider
  =hidden_field_tag :uid, @uid